---
name: resolve-review
description: Resolves GitHub review comments. Used only with slash command.
---

Use GitHub CLI (`gh`).

Reply to all addressed review comments with:
```
${short_commit_hash} で対応しました！
```

Then resolve those comments.
